apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: equipment-api
  name: equipment-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: equipment-api
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: equipment-api
    spec:
      containers:
      - image: nuget.czhipu.com/xnebula/hymson.equipment.api:20240724081238
        name: equipment-api
        volumeMounts:
          - mountPath: /logs
            name: equipment-api-volume
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        resources: {}
        ports: 
          - name: equipment-api-svc        #端口的名称
            containerPort: 80  #容器需要监听的端口号
            hostPort: 12003
      volumes:
      - name: equipment-api-volume
        # 挂载 /data/foo，但仅当该目录已经存在时
        hostPath:
          path: /data/logs # 主机上的目录位置
          type: DirectoryOrCreate # 此字段可选
status: {}